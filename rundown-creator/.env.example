# Rundown Creator Environment Variables
NODE_ENV=development
RUNDOWN_PORT=3001

# Database (same as main VidPOD)
DATABASE_URL=postgresql://username:password@host:port/database

# Main VidPOD API for auth proxy and data integration
VIDPOD_API_URL=http://localhost:3000/api

# Optional: API key for internal service communication
RUNDOWN_API_KEY=your-secure-api-key